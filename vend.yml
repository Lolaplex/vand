# Machine-readable hooks for vend.
version: 1
install:
  - python -m pip install -e .
  - python vend.py install-skills
update:
  - python -m pip install -e .
  - python vend.py install-skills
verify: python -m unittest discover -s tests -v
