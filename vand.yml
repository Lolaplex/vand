# Machine-readable hooks for vand.
version: 1
install:
  - python -m pip install -e .
  - python vand.py install-skills
update:
  - python -m pip install -e .
  - python vand.py install-skills
verify: python -m unittest discover -s tests -v
